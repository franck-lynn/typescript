# tsconfig.json 说明

是 笔记/typescript/tsconfig文件.md 文件的一些补充

```json
{
    "compilerOptions": {
        // 如 import xxx from '../src/...'
        // 在使用 baseUrl 后, 可以这样使用
        // import xxx from 'src/...'
        "baseUrl": "./"
    }
    
}
```

